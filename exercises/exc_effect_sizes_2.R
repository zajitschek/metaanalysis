###CODEBLOCK 4###

#Load libraries
library(compute.es)

#single calculation:
a.fes(f = __, n.1 = __, n.2 = __, R = 0, q = 0) #where we enter F-value, 
#sample sizes of treatment and control group, covariate correlation and 
# number of covariates (we have none in ANOVA))


